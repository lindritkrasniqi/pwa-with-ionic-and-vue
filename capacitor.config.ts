import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pwa-with-ionic-and-vue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
